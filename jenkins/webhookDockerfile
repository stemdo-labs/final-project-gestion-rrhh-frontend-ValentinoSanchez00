pipeline {
    agent any
    triggers {
        pollSCM('')
    }
    stages {
        stage('Build') {
            when {
                changeset "**/package.json"
                changeset "**/Dockerfile"
                changeset "**/webhookDockerfile"
            }
            steps {
                echo "Solo construyo si hay cambios en mi carpeta"
                
            }
        }
    }
}
